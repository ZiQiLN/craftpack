# craftpack

### Usage

```
yarn add webpack craftpack
```

```js
const configure = require("craftpack");

module.exports = configure({
  entry: {
    app: "./src/index.js"
  }
  // Other Overrides
});
```
